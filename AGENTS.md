# SmartHR-Agent-all 项目协作规则

本文件是本仓库的强制协作规范。任何进入本仓库工作的开发者或 AI 助手，都必须先阅读并遵守本文件。

## 一、开始工作前的强制检查

1. 开始任何新功能开发前，必须先完整阅读：
   - `docs/项目进度.md`
   - `docs/开发工作流规范.md`
   - 与本次功能直接相关的需求、设计和接口文档。
2. 检查 `docs/项目进度.md` 中的“当前开发项”和“最近功能记录”。
3. 只有当上一个功能状态为“已收口”，或当前没有未收口功能时，才允许开始下一个功能。
4. 如果上一个功能为“进行中”“待验收”“阻塞”或其他非“已收口”状态，必须暂停新功能开发，并向用户说明：
   - 尚未收口的功能名称；
   - 当前状态；
   - 尚未完成或尚待确认的事项；
   - 是否仍然确定开始下一个功能。
5. 在得到用户明确确认前，不得为下一个功能创建分支、修改代码或改变项目状态。

## 二、需求沟通规则

1. 对需求目标、业务规则、边界条件、交互方式、数据结构或验收标准存在不理解、歧义或冲突时，必须立即询问用户。
2. 不得在会明显影响产品方向、数据模型、接口契约或用户体验时自行猜测需求。
3. 可以在开发过程中随时提问；应优先把问题集中、清晰地说明，并给出已知背景和影响。
4. 开发前应明确本次功能的范围、不做什么以及可验证的验收条件。

## 三、新功能分支规则

本节只在正式进行功能开发时执行。仅讨论方案、整理文档或建立规范时，不要求创建功能分支，除非用户明确要求。

1. 每个新功能必须使用独立分支开发。
2. 创建分支前必须完成进度检查，并确认上一个功能已经收口。
3. 默认从最新的主分支创建功能分支。
4. Codex 创建的分支默认使用 `codex/` 前缀，建议格式：
   - `codex/feature-功能英文短名`
   - `codex/fix-问题英文短名`
   - `codex/docs-文档英文短名`
5. 一个分支聚焦一个功能或一组强相关改动，不混入无关修改。
6. 创建分支后，应立即在 `docs/项目进度.md` 中登记分支、功能范围和状态。

## 四、版本控制文件范围

1. Git 中应保留：
   - 项目源码；
   - `docs/` 项目文档；
   - `AGENTS.md` 等仓库级协作规则；
   - 构建、依赖和应用配置文件；
   - `.env.example`、`.env.template`、`.env.sample` 及其分环境变体等环境模板；
   - 数据库迁移、测试源码和必要的静态资源。
2. Git 中不得提交：
   - 真实 `.env` 和包含密钥、密码、Token、证书的本地配置；
   - 依赖目录、编译结果、打包产物和自动生成文件；
   - 日志、缓存、覆盖率报告、测试运行产物和临时文件；
   - IDE、编辑器和操作系统生成的个人文件；
   - 本地数据库、上传文件和运行时数据。
3. 新增技术栈或工具后，必须检查并补充根目录 `.gitignore`。
4. 提交前必须执行 `git status`，确认没有敏感信息和无关文件进入暂存区。
5. 环境模板只能包含变量名、示例值或安全占位符，不得包含任何真实凭据。

## 五、提交与推送规则

1. Git 提交信息必须使用中文，准确描述本次改动。
2. 与推送、合并、交付相关的说明必须使用中文。
3. 提交应保持单一目的，避免把多个无关功能放在同一个提交中。
4. 推荐提交格式：
   - `feat(候选人): 完成候选人档案创建功能`
   - `fix(面试): 修复面试时间冲突校验错误`
   - `docs(项目): 更新项目进度和验收结果`
   - `test(职位): 补充职位状态流转测试`
   - `refactor(权限): 重构数据权限校验逻辑`
5. 提交或推送前至少完成：
   - 检查本次改动范围；
   - 执行与改动风险相匹配的测试或检查；
   - 更新 `docs/项目进度.md`；
   - 确认没有误提交密钥、隐私数据、构建产物或无关文件。
6. 未经用户要求或授权，不主动提交、推送或合并代码。

## 六、合并规则

1. 功能完成并通过验收后，再合并到目标分支。
2. 默认使用非快进合并，以保留清晰的功能分支历史：

   ```bash
   git merge --no-ff <功能分支>
   ```

3. 合并提交信息使用中文，并说明合并的功能名称。
4. 合并前必须确认：
   - 验收条件已经满足；
   - 测试或必要检查已经通过；
   - 相关文档已经同步；
   - `docs/项目进度.md` 已准备更新为“已收口”。
5. 合并完成后，在进度文档中记录合并时间、目标分支、最终提交和遗留事项。

## 七、进度文档同步规则

1. `docs/项目进度.md` 是判断项目当前状态的唯一进度入口。
2. 每一批实际改动完成后，都必须同步更新进度文档，不能只修改代码而不更新进度。
3. 至少在以下节点更新进度：
   - 功能准备开始；
   - 分支创建完成；
   - 需求范围或验收标准发生变化；
   - 完成一个重要开发步骤；
   - 测试完成或发现阻塞问题；
   - 功能进入待验收；
   - 功能完成合并并正式收口。
4. 进度记录必须包含功能名称、状态、分支、已完成内容、待办事项、验证结果和最后更新时间。
5. 文档更新应与对应代码改动处于同一批次，必要时放入同一个提交。

## 八、功能状态定义

| 状态 | 含义 | 是否允许开始下一功能 |
| --- | --- | --- |
| 未开始 | 已登记，但尚未创建分支或修改代码 | 否，除非取消该功能或用户明确确认切换 |
| 进行中 | 正在开发或补充测试、文档 | 否 |
| 阻塞 | 因需求、技术或外部条件无法继续 | 否，必须先询问用户 |
| 待验收 | 开发与自测完成，等待用户确认 | 否，必须先询问用户 |
| 已收口 | 验收、文档和必要合并均已完成，无未处理的必做事项 | 是 |
| 已取消 | 用户明确取消，不再继续 | 是 |

## 九、功能收口标准

功能只有满足以下条件后，才能在进度文档中标记为“已收口”：

1. 约定范围内的功能已经实现。
2. 验收条件有明确验证结果。
3. 必要测试已经通过，未通过项已说明并得到用户接受。
4. 需求、接口、架构或使用说明等相关文档已经同步。
5. `docs/项目进度.md` 已记录最终状态和遗留事项。
6. 需要合并时，已按 `--no-ff` 完成合并；尚未要求合并时，应明确记录所处分支和交付状态。
7. 不存在未说明的高优先级问题。

## 十、推荐执行顺序

1. 阅读进度文档和相关资料。
2. 判断上一个功能是否已收口。
3. 发现歧义时立即询问用户。
4. 明确范围和验收标准。
5. 正式开发时创建独立分支。
6. 在进度文档登记新功能。
7. 实现功能并进行验证。
8. 每批改动后同步进度文档。
9. 进入“待验收”，由用户确认。
10. 按需提交、推送，并使用 `--no-ff` 合并。
11. 更新状态为“已收口”。
